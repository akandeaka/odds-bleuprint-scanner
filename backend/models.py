# Placeholder for future database models (e.g., SQLAlchemy)

class Fixture:
    def __init__(self, name, home, draw, away):
        self.name = name
        self.home = home
        self.draw = draw
        self.away = away
